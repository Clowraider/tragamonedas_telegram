{
  "name": "@slot-machine/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/admin/ui && cp -r src/admin/ui/* dist/admin/ui/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "node dist/db/migrate-cli.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/sensible": "^6.0.5",
    "@fastify/static": "^10.1.3",
    "@slot-machine/contracts": "^0.1.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.12.0",
    "pg": "^8.12.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.6",
    "typescript": "^5.5.0"
  }
}
